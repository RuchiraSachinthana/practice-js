const car = {
    make: "Honda",
    model: "Fit",
    year: 2020,
    engine: {
        capaacity: 3.6,
        type: "v6"
    }
}

const {
    model,
    engine
} = car;
console.log(model);
console.log(engine);

